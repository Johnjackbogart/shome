services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_USER: shome
      POSTGRES_PASSWORD: shome
      POSTGRES_DB: shome
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
